export class Car{

    // brand: string;
    // model: string;
    // variant: string;
    // color: string;
    // price: number;
    // year: Date;
    // state: string;

    constructor(
        brand: string,
        model: string,
        variant: string,
        color: string,
        price: number,
        year: Date,
        state: string
    ){}

}